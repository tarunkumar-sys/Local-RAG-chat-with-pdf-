"use client";

import { useState } from "react";

const Dashboard = () => {
  const [searchInput, setSearchInput] = useState<string>();

  return (
    <div>
    </div>
  );
};

export default Dashboard;
